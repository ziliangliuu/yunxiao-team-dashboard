# 工作统计 — 需求与实现说明

> 状态：持续维护中。当前扩展版本见 `extension/manifest.json` 的 `version` 字段（每次代码改动会 +0.1）。

## 一、背景

最初是 SEG 组长需要管理多个 aliyun 云效项目（电力交易 / 微电网 / 分布式集控 等）的方案设计、产品设计工作，按"迭代"维度统计组内成员的需求/任务/缺陷工作量。

后来项目列表改成**动态拉取**（不再硬编码），任何登录 devops.aliyun.com 的人都可以在自己的工作空间下直接使用。

默认成员名单来自本地私有配置 `extension/dashboard/config.local.js`（gitignored，模板见 `config.local.example.js`）；没有配置时**不过滤成员，展示全部人员**。也可在 UI 的"成员设置"页勾选保存。

## 二、项目列表

不再硬编码。仪表盘加载时调用 devops 接口 `GET /projex/api/workspace/project/search/list`（`extraConditions` 只保留 `scope=public`），返回当前工作空间下所有未归档的公开项目，按 `identifier` / `name` 解析后驱动所有卡片、图表、明细表。

工作空间 ID 也是动态的——由 `background.js` 从真实 API 请求的 `last-workspace` 头里抓取存储；兜底值可在本地私有配置 `config.local.js` 里设置（默认为空）。

## 三、实现形态

**Chrome 浏览器扩展（Manifest V3）**，复用浏览器对 devops.aliyun.com 的登录态调用 API，无需单独认证。

- 点扩展图标 → 新标签页打开仪表盘
- **鉴权自举**：仪表盘加载（及每次「刷新」）时直接 `fetch` 云效 `/projex` 页面（带 cookie），从服务端渲染的 HTML 里正则提取 `csrfToken` 和 workspaceId 存入 `chrome.storage.session`（失败降级 `local`）——只要浏览器登录过云效，打开插件即出数据，无需先访问云效页面
- `background.js` 的 `webRequest.onSendHeaders` 被动抓包（`x-csrf-token` / `last-workspace`）保留为兜底
- 仪表盘页面用 `fetch`（`credentials: include`）直接调 devops API，cookie 由浏览器自动携带

## 四、已识别的关键 API

域名：`https://devops.aliyun.com`

### 项目列表（新增）
- `GET /projex/api/workspace/project/search/list?extraConditions={...scope=public}&conditions={"conditionGroups":[[]]}&scope=all&category=Project&toPage=1&pageSize=200&_input_charset=utf-8`
- 响应：`{ code:200, result:[ {identifier, name, type, logicalStatus, ...} ] }`

### 工作项列表 / 计数
- 列表：`POST /projex/api/workitem/workitem/list?_input_charset=utf-8`
  ```json
  {
    "spaceType": "Project",
    "spaceIdentifier": "<projectId>",
    "category": "Req",          // Req | Task | Bug
    "toPage": 1,
    "pageSize": 200,
    "conditions": "{\"conditionGroups\":[]}",
    "searchType": "LIST"
  }
  ```
- 计数：`POST /projex/api/workitem/workitem/list/count?_input_charset=utf-8`（请求体同上，无 toPage/pageSize）

必要请求头：`x-csrf-token`、`last-workspace`、`web-last-workspace`、`accept: application/json, text/plain, */*`、`content-type: application/json`、`x-requested-with: XMLHttpRequest`。

工作项关键字段：`identifier`、`serialNumber`、`subject`、`status.name`、`statusStage.name`、`assignedTo.displayName`（负责人）、`ak.issue.member`（**参与人**，云效系统字段「参与者」，平铺在响应顶层、是用户对象数组，可能为空/缺失）、`sprint`、`gmtCreate`、`finishTime`、`workitemType.displayName`、`categoryIdentifier`。

### 测试计划（testhub，已通过浏览器抓包确认）
测试不在 workitem 体系里，走独立的 testhub webapi：
- 列表：`GET /testhub/webapi/workspace/project/{projectId}/testPlan/list?name=&category=TestPlan&toPage=1&pageSize=200&_input_charset=utf-8`（不传 `status` 参数即返回全部状态；云效页面自身传 `status=TODO,DOING`）
- 进度：`GET /testhub/webapi/workspace/testPlan/{planId}/progressRate` → `{ paasCount, failureCount, postponeCount, todoCount }`
- 详情深链：`https://devops.aliyun.com/projex/project/{projectId}/testplan/{identifier}`

测试计划关键字段：`identifier`、`name`、`statusName`（TODO/DOING/DONE）、`managers`（**多个**负责人）、`creator`、`sprint`、`gmtCreate`、`startDate`、`endDate`。`api.js` 把它转换成 workitem 形状（状态映射：TODO→未开始、DOING→进行中、DONE→已完成；statusStage：DONE→正常结束，其余→进行中；用例进度拼进标题），复用仪表盘现有管线。项目未开通测试模块（如 SEG）时列表接口报错，按空列表处理。

## 五、功能与口径

1. **类别 Tab**：全部 / 需求 / 任务 / 测试 / 缺陷。「全部」= 四个类别的合并视图，深链按每条工作项自身的 `categoryIdentifier` 生成；**页面打开默认就是「全部」**（首屏会顺序拉取四个类别）。切 Tab 全量拉取该类别 N 个项目的数据，**项目间并发**（「全部」下四个类别也并发；项目内分页顺序拉）；同一会话内已拉取过的类别会复用，「刷新」强制重拉。「测试」走 testhub 接口（见第四节），按负责人过滤时测试计划的多个 `managers` 命中任意一个即算。
2. **筛选维度是「迭代」（Sprint），不是日期**。迭代名形如 `2026-05迭代`，对应 `sprint` 字段。下拉框选项从已加载数据动态生成，第一项固定是 `全部`，**默认选中"全部"**；含 `<无迭代>` 选项。
3. **人员字段有「负责人」和「参与人」**：负责人 = aliyun API 的 `assignedTo`（单人）；参与人 = `ak.issue.member`（云效「参与者」，多人，测试计划无此概念）。涉及"按人筛选"的口径统一用 **负责人∪参与人**（`getPersonNames`），即选中某人时，ta 是负责人或参与人之一即命中。
4. **成员过滤**：卡片/图/表只展示选中组员（按 **负责人∪参与人** 匹配）的工作项。
   - 名单为空（默认）时不过滤、展示全部人员；可在「成员设置」页改（搜索 / 全选 / 反选 / 清空 / 恢复默认 / 保存），"恢复默认"恢复为 `config.local.js` 里配置的名单（未配置则为空）
   - 成员列表自动从加载过的工作项里聚合（creator/modifier/assignedTo/参与人 `ak.issue.member`）
   - 选择持久化到 `chrome.storage.local`
5. **顶部筛选栏**（叠加生效于卡片/图/表）：
   - 迭代：动态下拉，含"全部"，**默认"全部"**
   - 完成状态：全部 / 未关闭 / 已完成 / 已取消（异常结束）
   - 责任人：全部（团队 N 人）/ 单选某位组员。**口径为 负责人∪参与人**——选中某人即展示 ta 作为负责人或参与人的工作项（下拉标签仍叫「责任人」；候选名单也聚合两类人）
6. **三个数字（按当前筛选）**：
   - 总数 = 过滤后工作项数
   - 已完成 = `statusStage.name === "正常结束"`
   - 未关闭 = `statusStage.name NOT IN ["正常结束", "异常结束"]`
   - 说明：`logicalStatus` 恒为 NORMAL 不可用；`finishTime` 仅在终态阶段设置，故用 `statusStage` 判定。
7. **状态分布图**：按 `status.name` 横向柱状，按状态语义着色，每个项目一栏并列。
8. **明细表**：列 = 项目 / 编号 / 标题 / 类型 / 状态 / 负责人 / **参与人** / 迭代 / 创建时间 / 完成时间。所有单元格**不换行**，超宽省略号截断（标题列、参与人列悬停 title 看全文）。参与人多人时用「、」拼接，无参与人留空。
   - 每列点击切换 升序 ↑ → 降序 ↓ → 取消 ↕（空值恒置末尾）
   - 每行可点击，新标签页打开工作项详情：`https://devops.aliyun.com/projex/project/{projectId}/{req|task|bug}/{identifier}`

## 六、项目结构

```
20260514-seg部门工作统计/
├── extension/                       # 交付物：Chrome 扩展
│   ├── manifest.json                # MV3 配置（webRequest/storage/cookies + host_permissions）
│   ├── background.js                # 抓 csrf-token + last-workspace + 点图标开仪表盘
│   ├── README.md                    # 安装/使用说明
│   ├── icons/                       # 扩展图标
│   └── dashboard/
│       ├── index.html               # 仪表盘（Tab + 筛选栏 + 卡片/图/表）
│       ├── index.css                # 设计系统（CSS 变量）
│       ├── index.js                 # 主逻辑（过滤/排序/渲染/行跳转）
│       ├── api.js                   # devops API 封装 + 项目列表拉取 + 成员存储 + 语义函数
│       ├── members.html             # 成员设置页
│       └── members.js               # 成员设置逻辑
└── 需求.md                          # 本文档
```

## 七、安装与使用

1. Chrome → `chrome://extensions/` → 开启开发者模式 → 加载已解压扩展 → 选 `extension/` 目录
2. 浏览器登录 devops.aliyun.com（无需打开任何项目页，鉴权自举会自动获取 csrf-token 和工作空间 ID）
3. 点扩展图标 → 仪表盘新标签页打开
4. 顶部切 Tab / 迭代 / 完成状态 / 责任人；点表头排序；点行打开工作项
5. 顶栏「成员设置」管理统计成员名单，保存后回仪表盘点「刷新」生效
6. 「刷新」按钮 = 重新校验 csrf-token → 重新拉项目列表 → 重新拉当前类别的工作项。**不需要整页刷新扩展**；如果 csrf 过期，先去 devops 页面让背景脚本重新捕获一次，再回来点「刷新」即可

## 八、分享给别人能否直接用

可以，但他们需要注意：

- 他必须有 devops.aliyun.com 账号并已登录到自己的工作空间
- 装好扩展后**先打开自己工作空间下任意一个项目页**，让 `background.js` 抓到自己工作空间的 csrf-token / workspaceId
- 默认不过滤成员（展示全部人员）；想只看自己团队，进**"成员设置"**勾选保存，或配置 `config.local.js`
- 当前只拉公开（`scope=public`）项目；私有项目暂时看不到
- 未上架 Chrome 商店，对方所在企业的浏览器策略可能禁用"开发者模式加载"

交付方式：把整个 `extension/` 目录打 zip 给对方。

## 九、已知限制

- csrf-token 来自最近一次 devops API 请求头，长时间不操作可能失效，刷新一次 devops 页面即可重新捕获。
- 全量拉取 + 前端过滤，单项目工作项过多时首次加载较慢（pageSize=200 分页）。
- 工作项详情深链格式为 aliyun 云效标准格式，若个别项目路由不同需按实际调整。
- 「测试」Tab 展示的是**测试计划**（TestPlan）粒度，不下钻到单条测试用例；用例数量以"通过/失败/待测/暂缓"汇总拼在标题里。测试计划没有 `serialNumber`，编号列为空。
- 仅拉取 `scope=public` 项目；如果团队需要私有项目，得放开 `fetchProjects` 里的 extraConditions。
