# 更新日志

本项目采用简化的版本约定：**每次修改 `extension/` 下代码，`manifest.json` 的 `version` 加 0.1**（minor +1，patch 归 0）。每次发布在此记录一条。

## [0.16.0] - 2026-06-11

### 变更
- 扩展更名为「**工作统计**」（原「SEG 部门工作统计」）；顶栏品牌、窗口标题、工具栏提示同步更新。

### 新增
- 全新扩展图标：青绿柱状图 + 上升趋势箭头，提供 16 / 32 / 48 / 128 多尺寸（`extension/icons/`），并在 `manifest.json` 注册为扩展图标与工具栏图标。
- 顶栏左上角 logo 改用真实图标（原为字母占位块）。
- 图标可复现生成脚本 `tools/generate-icon.py`（纯 Pillow，无外部依赖）。
- 本更新日志 `CHANGELOG.md`。

### 隐私
- 复核公开仓库（GitHub）：当前 `main` 与远端均**不含**真实姓名 / 工作空间 ID；这些数据仅残留在本地历史（旧 `v1.0` 标签 / `local-history` 分支），从未推送。
- 安装本地 `pre-push` 守卫钩子，拦截任何含敏感信息的提交被推到远端（敏感信息只能存在本地）。
- 扩展 `.gitignore`：`config.local.js` 仅存本地、不入库，并忽略 `*.zip` 与 AI 工具脚手架目录。

## [0.15.0] 及更早

- 仪表盘核心：五个 Tab（全部 / 需求 / 任务 / 测试 / 缺陷）、迭代 / 完成状态 / 责任人三重筛选、项目卡片与状态分布图、九列可排序明细表、成员设置页、`background.js` 鉴权自举（捕获 csrf-token 与工作空间 ID）。
